### Example environment variables (DO NOT COMMIT)
# Copy this to `.env.local` and replace the placeholder with your real API key.
# Keep secrets out of version control.

# Vite exposes variables prefixed with VITE_ to client code. For local dev set:
VITE_GOOGLE_API_KEY=your_google_genai_api_key_here

# Example (bash):
#   echo "VITE_GOOGLE_API_KEY=abcd..." > .env.local
